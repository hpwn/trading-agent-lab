# ==============================
# Runtime basics
# ==============================
ENV=dev
TZ=America/New_York

# Starting capital used for eval-to-dollars conversion (paper & sim displays)
CAPITAL=10000

# Optional: default symbol for quick demos/backtests
# DATA_SYMBOL=SPY

# ==============================
# Storage (optional; YAML may also set these)
# ==============================
DB_URL=sqlite:///./lab.db
ARTIFACTS_DIR=./artifacts

# ==============================
# Broker selection & execution
# ==============================
# Safe default is paper trading via Alpaca. Other options: alpaca_real, sim
LIVE_BROKER=alpaca_paper

# Dry-run switch for live runs: 0 = no orders placed, 1 = place orders
LIVE_EXECUTE=0

# Enable Alpaca paper extended-hours routing (orders will be DAY + LIMIT).
# Set to 1/true/yes/on to enable; leave unset/0/false to disable.
# Note: Alpaca still enforces session windows; outside those, orders will be rejected upstream.
# ALLOW_AFTER_HOURS=0

# Per-order notional guardrail (USD). Examples:
#   LIVE_MAX_ORDER_USD=10000   -> cap to $10k per order
#   LIVE_MAX_ORDER_USD=none    -> disable the guardrail entirely (not recommended)
# If unset, the adapter uses its YAML-configured cap (e.g., live.max_order_usd).
# LIVE_MAX_ORDER_USD=10000

# If a cap is in effect, automatically clip orders down to the cap (1) instead of erroring (0).
# CLIP_ORDER_TO_MAX=0

# Daily loss stop (percent). Trading halts for the day if exceeded.
DAILY_MAX_LOSS_PCT=1

# ==============================
# Real trading hard gate
# ==============================
# Must be truthy (true/1/yes/on) to allow *any* real orders when LIVE_BROKER=alpaca_real.
# KEEP THIS BLANK unless you are absolutely sure.
REAL_TRADING_ENABLED=

# ==============================
# Alpaca credentials & options (paper by default)
# ==============================
# Fill these from your Alpaca dashboard (use a Paper account for safety).
ALPACA_API_KEY_ID=
ALPACA_API_SECRET_KEY=

# Optional: choose market data feed, e.g. 'iex' (default) or 'sip'
ALPACA_FEED=iex

# Optional: override hosts (normally you do NOT need these)
# TRADING endpoint override (paper default: https://paper-api.alpaca.markets)
# ALPACA_BASE_URL=https://paper-api.alpaca.markets
# DATA host override (SDK picks the right one by default)
# ALPACA_DATA_URL=https://data.alpaca.markets

# ==============================
# Achievements (optional)
# ==============================
ACHIEVEMENTS_ENABLED=1
ACHIEVEMENTS_DIR=./artifacts/achievements
# ACHIEVEMENTS_PROFIT_SOURCE=eval  # options: eval, live, both
